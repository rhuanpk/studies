>>> POSTGRES <<<
docker volume create postgres
docker run -d --name postgres -e POSTGRES_PASSWORD=postgres [-v [/path/]postgres:/var/lib/postgresql/data] postgres

>>> ADMINER <<<
docker run -d --rm --name adminer [--link database:db] -p 80:8080 adminer
