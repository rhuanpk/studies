# REST API In a Single Package Main

## New Endpoint

To setup a new endpoint, follow the example in `internal/server`, like the `test` folder and put the new route in `internal/process/inits/inits.go`.
